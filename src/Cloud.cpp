//
// Created by tadeze on 12/2/16.
//

#include "Cloud.h"
/*
Cloud::Cloud(std::vector<int> &index, std::vector<std::vector<double> > &points) {
    float x=0.0,y=0.0,z=0.0;
    for(int i=0;i<index.size();i++)
    {
        x+=points[index[i]][0];
        y+=points[index[i]][1];
        z+=points[index[i]][2];

    }
    centerX = x;
    centerY= y;
    centerZ= z;
    observations = index;

}


*/